# Zero Mesh (Linux)

A decentralized, cryptographic mesh networking protocol for secure, air-gapped communications.

This is the highly-optimized native Linux core. It statically links internal dependencies for maximum zero-latency performance.